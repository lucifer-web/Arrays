#include <bits/stdc++.h>
using namespace std;
int main() {
    string s;
    getline(cin,s);
    int h[27]={0};
    for(int i=0;s[i]!='\0';i++){
        h[s[i]-'a']++;
    }
    int c=0;
    for(int i=0;i<27;i++){
       if(h[i]>0) c++;
    }
    cout<<c;
}
